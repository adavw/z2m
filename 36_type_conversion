#Use input() to receive a year and calculate a person's age.

#input() assigns input value as str to birth_year
birth_year = input('What year where you born? ')

#calculate age by subtracting birth_year (converted to int) from current year.
age = 2020 - int(birth_year)

#use a formatted string (chapter 31) to output the value of age.
print(f'{age}')
